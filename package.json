{
  "name": "WikiSkimmer",
  "version": "0.0.1",
  "description": "Test app",
  "main": "index.js",
  "scripts": {
    "start": "electron .",
  	"build-mac": "electron-packager . WikiSkimmer --platform=darwin --arch=x64 --ignore=node_modules/electron-* --ignore=data/*",
  	"build-win": "electron-packager . WikiSkimmer --platform=win32 --arch=ia32 --ignore=node_modules/electron-* --ignore=data/*",
  	"build-linux": "electron-packager . WikiSkimmer --platform=linux --arch=ia32 --ignore=node_modules/electron-* --ignore=data/*"
  },
  "author": "Ajani James Bilby",
  "license": "ISC",
  "devDependencies": {
    "electron-packager": "^7.0.2",
    "electron": "^1.1.1"
  }
}
